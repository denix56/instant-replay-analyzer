{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the native UI shell",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
